# === Core runtime ===
fastapi==0.120.0           # modern FastAPI w/ Pydantic v2 support
pydantic==2.11.0           # models & validation
uvicorn[standard]==0.38.0  # ASGI server (+uvloop, httptools, watchfiles, etc.)
sentence-transformers==5.1.1
faiss-cpu==1.12.0
PyMuPDF==1.26.5
pandas==2.2.2
pyarrow==21.0.0

# === Optional UI (comment out if you don’t want Streamlit) ===
streamlit==1.50.0

# === Lexical baseline (BM25) ===
# Disk-backed inverted index; replaces rank-bm25 whose per-document token
# arrays consumed too much RAM at the dissertation's ~6.6k chunk scale.
tantivy==0.25.0

# === Eval harness ===
requests>=2.28.0
arxiv==2.1.3               # used by download_papers.py to fetch cs.IR / cs.CL PDFs
scipy>=1.10.0              # Wilcoxon signed-rank tests in eval.py
matplotlib>=3.8.0          # publication plots in plot_results.py
seaborn>=0.13.0            # theming for plot_results.py

# === Dev & test ===
pytest==8.4.2
black==25.9.0
ruff==0.14.1
